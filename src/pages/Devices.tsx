
import { AppLayout } from "@/components/app/app-layout";
import { DevicesContent } from "@/components/app/devices-content";

const Devices = () => {
  return (
    <AppLayout>
      <DevicesContent />
    </AppLayout>
  );
};

export default Devices;
