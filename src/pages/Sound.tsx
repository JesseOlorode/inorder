
import { AppLayout } from "@/components/app/app-layout";
import { SoundContent } from "@/components/app/sound-content";

const Sound = () => {
  return (
    <AppLayout>
      <SoundContent />
    </AppLayout>
  );
};

export default Sound;
