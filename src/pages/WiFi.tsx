
import { AppLayout } from "@/components/app/app-layout";
import { WiFiContent } from "@/components/app/wifi-content";

const WiFi = () => {
  return (
    <AppLayout>
      <WiFiContent />
    </AppLayout>
  );
};

export default WiFi;
