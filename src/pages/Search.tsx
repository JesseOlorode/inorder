
import { AppLayout } from "@/components/app/app-layout";
import { SearchContent } from "@/components/app/search-content";

const Search = () => {
  return (
    <AppLayout>
      <SearchContent />
    </AppLayout>
  );
};

export default Search;
