
import { AppLayout } from "@/components/app/app-layout";
import { FavoritesContent } from "@/components/app/favorites-content";

const Favorites = () => {
  return (
    <AppLayout>
      <FavoritesContent />
    </AppLayout>
  );
};

export default Favorites;
